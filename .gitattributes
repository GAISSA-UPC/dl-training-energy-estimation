# Auto detect text files and perform LF normalization
* text=auto

*.pkl filter=lfs diff=lfs merge=lfs -text
*.gzip !text !filter !merge !diff
*.joblib !text !filter !merge !diff
*.html !text !filter !merge !diff
